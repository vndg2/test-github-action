pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {echo 'cloning...'}
        }
        stage('Build') {
            steps {echo 'building...'}
        }
        stage('Test') {
            steps {echo 'testing...'}
        }
    }

    post {

        always {echo 'done!'}
    }
}